import { WindowModel } from "../../Framework/Model/WindowModel";
import { WindowName } from "../../Framework/Window/WindowName";


export class BattleModel extends WindowModel {
    constructor(windowName: WindowName) {
        super(windowName);
    }
}
